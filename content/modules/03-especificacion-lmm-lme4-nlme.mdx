---
title: "Especificación de un LMM (Linear Mixed Model)"
slug: "especificacion-lmm-lme4-nlme"
subtitle: "Variantes del LMM, cuándo usar cada una, y cómo leer la salida completa del modelo."
objective:
  - "Distinguir y escribir las tres variantes fundamentales de la estructura aleatoria de un LMM."
  - "Entender qué aporta conceptualmente cada componente del modelo (intercepto, pendiente, correlación)."
  - "Ajustar y comparar variantes con lmer() y lme(), e identificar la equivalencia en SAS PROC MIXED."
  - "Leer e interpretar íntegramente la salida de summary(lmer()): efectos fijos, varianzas y residual."
  - "Aplicar criterios de datos y teoría para decidir qué estructura aleatoria especificar."
order: 3
datasets:
  - "sleepstudy (lme4)"
---

## Del análisis exploratorio a la especificación del modelo

El análisis exploratorio del módulo anterior reveló dos señales claras en `sleepstudy`:

1. **Los interceptos varían entre sujetos**: en el día 0, el tiempo de reacción se extiende desde aproximadamente 200 ms hasta más de 310 ms. Cada sujeto tiene un nivel basal propio.
2. **Las pendientes también varían**: algunos sujetos se deterioran apenas 5 ms por día de privación, mientras que otros empeoran a un ritmo de 25 ms por día.

Estas dos observaciones empíricas se traducen directamente en decisiones de especificación. Como señalan **Correa y Salazar (2025)** en su texto sobre modelos mixtos, *"la estructura de los efectos aleatorios debe reflejar las fuentes de variación identificadas en la etapa exploratoria; no hay una estructura universalmente correcta, sino una que es adecuada para los datos y la pregunta de investigación"*.

Esta sección formaliza ese razonamiento. Seguimos de cerca la exposición de **Fitzmaurice, Laird y Ware (2011, cap. 8)**, quienes enfatizan que la especificación de un modelo mixto es un proceso iterativo que combina criterios estadísticos, gráficos y teóricos.

---

## Las tres variantes fundamentales de la estructura aleatoria

Un LMM con respuesta $y_{ij}$ para el sujeto $i$ en el tiempo $j$ y un predictor continuo $t_{ij}$ admite tres configuraciones comunes de efectos aleatorios. Cada una responde a una pregunta diferente sobre la heterogeneidad de la población. Siguiendo la notación de **Correa y Salazar (2025, cap. 5)**, presentamos las variantes en orden creciente de complejidad.

<DataTable
  caption="Variantes de la estructura aleatoria de un LMM"
  columns={["Variante", "Fórmula aleatoria en R", "Parámetros en D", "Cuándo usarla"]}
  rows={[
    [
      "A — Solo intercepto",
      "(1 | Subject)",
      "1 (τ₀²)",
      "Perfiles aproximadamente paralelos en el spaghetti plot; solo varía el nivel basal entre sujetos"
    ],
    [
      "B — Intercepto + pendiente (correlación libre)",
      "(time | Subject)",
      "3 (τ₀², τ₁², τ₀₁)",
      "Perfiles que divergen o se cruzan; hay sujetos con tasas de cambio visiblemente distintas"
    ],
    [
      "C — Intercepto + pendiente (correlación = 0)",
      "(1 | Subject) + (0 + time | Subject)",
      "2 (τ₀², τ₁²)",
      "Como B, pero con pocos datos por sujeto o cuando la correlación estimada es inestable"
    ]
  ]}
/>

<Callout type="info" title="La notación (time | Subject) es abreviatura de (1 + time | Subject)">
  El intercepto aleatorio siempre está implícito cuando hay más de un término a la izquierda de `|`. Para excluir el intercepto aleatorio (algo que rara vez se justifica en la práctica) se escribe `(0 + time | Subject)`. **Fitzmaurice et al. (2011, p. 195)** señalan que los modelos sin intercepto aleatorio implican que todos los sujetos parten del mismo valor basal, un supuesto fuerte que debe verificarse.
</Callout>

---

## Variante A: solo intercepto aleatorio

### Qué dice conceptualmente

Este modelo asume que **todos los sujetos comparten la misma pendiente** (la misma tasa de cambio a lo largo del tiempo), pero cada uno parte de un **nivel basal distinto**. La heterogeneidad entre sujetos es exclusivamente de posición: los perfiles individuales son líneas paralelas, desplazadas verticalmente unas de otras.

$$
y_{ij} = (\beta_0 + b_{0i}) + \beta_1\, t_{ij} + \varepsilon_{ij}, \qquad b_{0i} \sim N(0, \tau_0^2), \qquad \varepsilon_{ij} \sim N(0, \sigma^2)
$$

La varianza total de $y_{ij}$ se descompone en dos fuentes:

- $\tau_0^2$: varianza **entre sujetos** (cuánto difieren los niveles basales).
- $\sigma^2$: varianza **intra-sujeto** (ruido residual no explicado).

Una cantidad derivada de gran utilidad es el **coeficiente de correlación intraclase** (ICC, por sus siglas en inglés), que mide la proporción de la varianza total atribuible a diferencias entre sujetos:

$$
\text{ICC} = \frac{\tau_0^2}{\tau_0^2 + \sigma^2}
$$

**Correa y Salazar (2025, cap. 4)** indican que un ICC superior a 0.10 ya justifica la inclusión del efecto aleatorio, pues refleja que al menos el 10 % de la variabilidad observada proviene de diferencias estables entre las unidades. Valores superiores a 0.50 son frecuentes en estudios con diseños cuidadosamente controlados donde la heterogeneidad entre individuos domina sobre el error de medición.

### Cuándo está justificada

- La exploración gráfica muestra perfiles **aproximadamente paralelos**: las líneas del spaghetti plot tienen pendientes similares y mantienen distancias relativas constantes.
- La teoría no sugiere que las unidades deban tener tasas de cambio distintas. Por ejemplo, si el efecto del tiempo es el mismo para todos los pacientes de un grupo homogéneo, una pendiente aleatoria no estaría respaldada conceptualmente, aunque los datos la sugirieran débilmente.

**Fitzmaurice et al. (2011, p. 192)** advierten que la variante de solo intercepto aleatorio es a menudo un excelente punto de partida, y que conviene resistir la tentación de añadir pendientes aleatorias sin una justificación clara.

### Código R y SAS

<RCell id="mod-ri" title="Variante A: solo intercepto aleatorio" code={`library(lme4)
data(sleepstudy)

# Variante A: solo intercepto aleatorio
mod_A <- lmer(Reaction ~ Days + (1 | Subject),
              data    = sleepstudy,
              REML    = TRUE)

summary(mod_A)

# ICC: fracción de varianza entre sujetos
vc <- as.data.frame(VarCorr(mod_A))
tau0_sq <- vc[vc$grp == "Subject", "vcov"]
sigma_sq <- vc[vc$grp == "Residual", "vcov"]

cat("ICC =", round(tau0_sq / (tau0_sq + sigma_sq), 3), "\n")`} />

<SASAccordion title="Variante A en SAS PROC MIXED" code={`/* Variante A: solo intercepto aleatorio */
PROC MIXED DATA=sleepstudy METHOD=REML COVTEST;
  CLASS Subject;
  MODEL Reaction = Days / SOLUTION DDFM=KENWARDROGER;
  RANDOM INTERCEPT / SUBJECT=Subject TYPE=VC;
  /* TYPE=VC: varianza constante — un solo parámetro tau0^2 */
RUN;

/* El output incluye:
   - "Covariance Parameter Estimates": tau0^2 y sigma^2
   - "Solution for Fixed Effects": beta0 y beta1 con sus errores estándar
   ICC = tau0^2 / (tau0^2 + sigma^2) se calcula manualmente */`} />

---

## Variante B: intercepto y pendiente aleatorios (correlación libre)

### Qué añade conceptualmente

Este modelo reconoce que las unidades **difieren tanto en su nivel basal como en su tasa de cambio**. Además, permite que ambas características estén relacionadas en la población. La pregunta sustantiva es: ¿los sujetos que parten de un valor más alto tienden también a cambiar más rápido, o por el contrario los que parten más alto cambian menos?

$$
y_{ij} = (\beta_0 + b_{0i}) + (\beta_1 + b_{1i})\, t_{ij} + \varepsilon_{ij}
$$

$$
\begin{pmatrix} b_{0i} \\ b_{1i} \end{pmatrix} \sim N\!\left(
  \begin{pmatrix}0\\0\end{pmatrix},
  \begin{pmatrix}\tau_0^2 & \tau_{01} \\ \tau_{01} & \tau_1^2\end{pmatrix}
\right)
$$

La **correlación entre efectos aleatorios**, definida como $\rho_{01} = \tau_{01}/(\tau_0\,\tau_1)$, tiene una interpretación sustantiva directa. Un $\rho_{01}$ positivo indica que los sujetos que parten más alto también cambian más rápido (en el caso de `sleepstudy`, se deterioran más). Un $\rho_{01}$ negativo sugiere convergencia: los que parten más alto cambian menos a lo largo del tiempo, acercándose a los demás.

**Correa y Salazar (2025, cap. 5)** dedican una sección completa a la interpretación de esta correlación, señalando que en estudios de crecimiento es frecuente encontrar correlaciones negativas (los individuos que parten más altos crecen más lentamente), mientras que en estudios de deterioro clínico pueden aparecer correlaciones positivas (los más afectados al inicio empeoran más rápido).

### Cuándo está justificada

- El spaghetti plot muestra **trayectorias que divergen o se cruzan** visiblemente.
- El análisis exploratorio reveló que la correlación individual entre el predictor temporal y la respuesta varía sustancialmente entre sujetos.
- Hay suficientes mediciones por sujeto para estimar dos parámetros aleatorios más su correlación. La regla práctica citada por **Fitzmaurice et al. (2011, p. 200)** es disponer de al menos 5 mediciones por unidad.
- La teoría sugiere que el punto de partida y la velocidad de cambio podrían estar relacionados.

### Código R y SAS

<RCell id="mod-ri-rs" title="Variante B: intercepto y pendiente aleatorios (D sin restricciones)" code={`library(lme4)
data(sleepstudy)

# Variante B: intercepto y pendiente aleatorios, correlación libre
# (1 + Days | Subject) equivale a (Days | Subject)
mod_B <- lmer(Reaction ~ Days + (Days | Subject),
              data = sleepstudy,
              REML = TRUE)

summary(mod_B)

# La sección "Random effects" muestra:
# - Std.Dev (Subject, Intercept): sqrt(tau0^2)  — dispersión de niveles basales
# - Std.Dev (Subject, Days):      sqrt(tau1^2)  — dispersión de pendientes
# - Corr:                         rho_01         — correlación entre ambos
# - Std.Dev (Residual):           sigma          — ruido intra-sujeto

# Matriz D estimada
VarCorr(mod_B)`} />

<SASAccordion title="Variante B en SAS PROC MIXED" code={`/* Variante B: intercepto y pendiente aleatorios, D sin restricciones (UN) */
PROC MIXED DATA=sleepstudy METHOD=REML COVTEST;
  CLASS Subject;
  MODEL Reaction = Days / SOLUTION DDFM=KENWARDROGER;
  RANDOM INTERCEPT Days / SUBJECT=Subject TYPE=UN G GCORR;
  /* TYPE=UN: matriz D sin restricciones — estima tau0^2, tau1^2 y tau01
     G: imprime la matriz D estimada
     GCORR: imprime la correlación rho01 entre intercepto y pendiente */
RUN;`} />

---

## Variante C: intercepto y pendiente aleatorios (correlación forzada a 0)

### Por qué forzar la correlación a cero

Estimar la correlación $\rho_{01}$ consume un parámetro adicional y, cuando hay pocos sujetos o pocas mediciones por sujeto, puede generar inestabilidad numérica. El mensaje `boundary (singular) fit` que a veces emite `lmer` suele estar relacionado con una matriz $\mathbf{D}$ difícil de estimar. En estas situaciones, forzar $\tau_{01} = 0$ produce un modelo más estable sin renunciar a la heterogeneidad en las pendientes:

$$
\mathbf{D} = \begin{pmatrix}\tau_0^2 & 0 \\ 0 & \tau_1^2\end{pmatrix}
$$

Esta restricción no niega que los efectos aleatorios existan; simplemente asume que el nivel basal y la tasa de cambio de un sujeto no están asociados en la población bajo estudio.

<Callout type="warning" title="Cuándo NO usar la Variante C">
  Si la teoría o el análisis exploratorio sugieren una relación entre el nivel basal y la pendiente (por ejemplo, pacientes más graves al inicio se recuperan más lentamente), forzar $\tau_{01} = 0$ produce un modelo mal especificado. En ese caso, es preferible conservar la Variante B y, si hay problemas de convergencia, cambiar el optimizador (`lmerControl(optimizer = "bobyqa")`) o escalar los predictores. **Correa y Salazar (2025, cap. 5)** recomiendan examinar el gráfico de los BLUPs de intercepto contra los BLUPs de pendiente para decidir si la correlación es necesaria.
</Callout>

### Código R y SAS

<RCell id="mod-ri-rs-indep" title="Variante C: intercepto y pendiente sin correlación" code={`library(lme4)
data(sleepstudy)

# Variante C: dos efectos aleatorios INDEPENDIENTES
# (1 | Subject) + (0 + Days | Subject) fuerza tau01 = 0
mod_C <- lmer(Reaction ~ Days + (1 | Subject) + (0 + Days | Subject),
              data = sleepstudy,
              REML = TRUE)

summary(mod_C)
VarCorr(mod_C)

# Notar: en "Random effects" ya no aparece la línea "Corr"`} />

<SASAccordion title="Variante C en SAS (TYPE=VC para ambas componentes)" code={`/* Variante C: intercepto y pendiente aleatorios, covarianza = 0 */
PROC MIXED DATA=sleepstudy METHOD=REML COVTEST;
  CLASS Subject;
  MODEL Reaction = Days / SOLUTION DDFM=KENWARDROGER;
  RANDOM INTERCEPT / SUBJECT=Subject TYPE=VC;
  RANDOM Days      / SUBJECT=Subject TYPE=VC;
  /* Dos sentencias RANDOM separadas con TYPE=VC fuerzan
     la covarianza entre intercepto y pendiente a cero */
RUN;`} />

---

## Cómo elegir entre variantes: criterios de datos y teoría

La elección de la estructura aleatoria no es un problema puramente estadístico. **Fitzmaurice et al. (2011, cap. 8)** insisten en que debe combinarse el juicio teórico con la evidencia empírica. **Correa y Salazar (2025, cap. 5)** proponen un esquema de decisión en tres planos que adaptamos a continuación.

<ProcessSteps steps={[
  {
    title: "Plano teórico: preguntarse si la heterogeneidad tiene sentido",
    description: "¿Es razonable que cada sujeto tenga su propia tasa de cambio? Si se mide el efecto de un fármaco en pacientes con la misma condición basal, quizás la pendiente aleatoria no está justificada conceptualmente, aunque los datos la sugieran. La teoría debe guiar la especificación; los datos, validarla."
  },
  {
    title: "Plano empírico: leer el spaghetti plot y la variabilidad de pendientes",
    description: "Perfiles paralelos → solo intercepto aleatorio (Variante A). Perfiles que divergen o se cruzan → considere pendiente aleatoria (Variante B o C). Complemente con el gráfico de pendientes individuales del módulo de exploración."
  },
  {
    title: "Plano estadístico: comparar modelos anidados",
    description: "Use anova(mod_A, mod_B, refit=FALSE) para una prueba de razón de verosimilitud (LRT) entre variantes anidadas. Complemente con AIC y BIC. Si el modelo más complejo no mejora significativamente, quédese con el más simple por parsimonia."
  },
  {
    title: "Plano numérico: verificar convergencia y estabilidad",
    description: "Si lmer() emite una advertencia de ajuste singular o de convergencia fallida con la Variante B, intente primero la Variante C (menos parámetros), luego cambie el optimizador a bobyqa, y como último recurso escale los predictores para mejorar el condicionamiento numérico."
  }
]} />

### Comparación estadística entre variantes

<RCell id="compare-variants" title="Comparar Variante A vs B con LRT y AIC/BIC" code={`library(lme4)
data(sleepstudy)

mod_A <- lmer(Reaction ~ Days + (1 | Subject),         data = sleepstudy, REML = TRUE)
mod_B <- lmer(Reaction ~ Days + (Days | Subject),       data = sleepstudy, REML = TRUE)
mod_C <- lmer(Reaction ~ Days + (1 | Subject)
                            + (0 + Days | Subject),     data = sleepstudy, REML = TRUE)

# LRT entre modelos anidados
# refit=FALSE mantiene REML: válido para comparar SOLO la estructura aleatoria
anova(mod_A, mod_B, refit = FALSE)

# Tabla de AIC / BIC para los tres
AIC(mod_A, mod_B, mod_C)
BIC(mod_A, mod_B, mod_C)`} />

**Cómo leer la comparación:**

- El **LRT** (estadístico $\chi^2$ y valor p en la salida de `anova()`) contrasta la hipótesis nula de que el modelo más simple es suficiente. Un valor $p < 0.05$ favorece la variante más compleja.  
  **Correa y Salazar (2025, cap. 5)** advierten que el LRT para componentes de varianza es conservador cuando el parámetro está en la frontera del espacio paramétrico ($\tau_1^2 = 0$), y recomiendan considerar también los criterios de información.
- El **AIC** penaliza la verosimilitud por el número de parámetros. El modelo con menor AIC es preferible cuando existe incertidumbre sobre la estructura correcta.
- El **BIC** penaliza más fuertemente la complejidad y suele favorecer modelos más parsimoniosos que el AIC. En muestras grandes, el BIC tiende a seleccionar el modelo verdadero si está entre los candidatos.
- Si el LRT y el AIC apuntan al modelo más complejo pero el BIC favorece al más simple, la decisión debe apoyarse en la **teoría** y en los patrones observados en los gráficos exploratorios.

<Callout type="warning" title="LRT con REML solo para comparar estructura aleatoria">
  `anova(mod_A, mod_B, refit=FALSE)` compara la estructura **aleatoria** manteniendo los mismos efectos fijos. Para comparar modelos con **distintos efectos fijos**, ambos deben ajustarse con `REML=FALSE`. Este punto se retoma con más detalle en el módulo de comparación de modelos.
</Callout>

---

## Guía completa de lectura del output de summary(lmer())

La salida de `summary()` de un modelo ajustado con `lmer` contiene cinco bloques de información. Saber leer cada uno es una competencia fundamental. **Fitzmaurice et al. (2011, cap. 8)** dedican un apartado completo a la interpretación de salidas de software, y adaptamos aquí sus recomendaciones.

<RCell id="output-guide" title="Output completo del modelo B con sleepstudy" code={`library(lme4)
data(sleepstudy)

mod_B <- lmer(Reaction ~ Days + (Days | Subject),
              data = sleepstudy, REML = TRUE)
summary(mod_B)`} />

La salida se estructura en cinco bloques. A continuación se detalla cada uno.

### Bloque 1 — Fórmula y método de estimación
```
Linear mixed model fit by REML ['lmerMod']
Formula: Reaction ~ Days + (Days | Subject)
Data: sleepstudy
REML criterion at convergence: 1743.6
```

**REML criterion**: la deviance REML, es decir, $-2 \times$ log-verosimilitud restringida. Solo es comparable entre modelos que comparten los mismos efectos fijos. No debe usarse para comparar modelos con distintas variables predictoras.
La línea `REML criterion at convergence` indica que el algoritmo de optimización convergió exitosamente. Si apareciera un mensaje de advertencia aquí, habría que revisar la especificación.

### Bloque 2 — Residuos escalados
```
Scaled residuals:
    Min      1Q  Median      3Q     Max
 -3.9536 -0.4634  0.0231  0.4634  5.1793
```

Los residuos se presentan escalados (divididos por su desviación estándar estimada). Si el modelo es adecuado:

- La mediana debería estar próxima a 0.
- El primer y tercer cuartil deberían ser aproximadamente simétricos ($\approx \pm 0.67$ para una distribución normal).
- El mínimo y el máximo no deberían alejarse excesivamente de $\pm 3.5$. Valores extremos (como el 5.18 que aparece aquí) pueden indicar la presencia de observaciones atípicas que conviene examinar.

### Bloque 3 — Efectos aleatorios ← **el corazón del LMM**

```
Random effects:
  Groups Name Variance Std.Dev. Corr
  Subject (Intercept) 612.10 24.741
  Days 35.07 5.922 0.07
  Residual 654.94 25.592
  Number of obs: 180, groups: Subject, 18
```

| Componente | Notación | Qué mide |
|---|---|---|
| Variance (Intercept, Subject) | $\tau_0^2 = 612$ | Cuánto varían los tiempos de reacción basales entre sujetos (en ms²) |
| Variance (Days, Subject) | $\tau_1^2 = 35$ | Cuánto varían las pendientes entre sujetos (en (ms/día)²) |
| Corr | $\rho_{01} = 0.07$ | Correlación entre intercepto y pendiente aleatorios; aquí es prácticamente nula |
| Residual | $\sigma^2 = 655$ | Variabilidad intra-sujeto no explicada por el modelo |

La **desviación estándar** (`Std.Dev.`) es la raíz cuadrada de la varianza y se expresa en las mismas unidades que la respuesta. Por ejemplo, `Std.Dev. (Intercept) = 24.7` ms significa que, aproximadamente, el 95 % de los sujetos tienen su intercepto dentro de $\pm 1.96 \times 24.7 \approx \pm 48$ ms alrededor del intercepto medio poblacional.

**Correa y Salazar (2025, cap. 5)** recomiendan prestar especial atención a la magnitud relativa de $\tau_1$ respecto a $\beta_1$: si la desviación estándar de las pendientes es grande en comparación con la pendiente media, existe una heterogeneidad sustancial en las trayectorias y el modelo con pendiente aleatoria está plenamente justificado.

### Bloque 4 — Efectos fijos
```
Fixed effects:
  Estimate Std. Error t value
  (Intercept) 251.405 6.825 36.84
  Days 10.467 1.546 6.77
```

- **Intercept (251.4 ms)**: tiempo de reacción promedio poblacional en el día 0 de privación.
- **Days (10.5 ms/día)**: por cada día adicional de privación de sueño, el tiempo de reacción promedio aumenta 10.5 ms.
- **Std. Error**: error estándar del estimador, que cuantifica la incertidumbre asociada.
- **t value**: el cociente entre la estimación y su error estándar. En muestras razonablemente grandes, valores superiores a 2 en valor absoluto indican significación estadística aproximada.

`lme4` no reporta valores p para los efectos fijos por defecto. Esto obedece a un debate metodológico sobre los grados de libertad apropiados en modelos con estructura aleatoria compleja o datos desbalanceados. Para obtener valores p, existen tres alternativas:

1. Usar `lmerTest::lmer()`, que incorpora la aproximación de Satterthwaite.
2. Calcular intervalos de confianza con `confint(mod_B, method = "Wald")`.
3. Usar la aproximación de Kenward-Roger disponible en el paquete `pbkrtest`.

### Bloque 5 — Correlación de los efectos fijos
```
Correlation of Fixed Effects:
  (Intr)
  Days -0.138
```

Esta es la **correlación entre los estimadores** $\hat{\beta}_0$ y $\hat{\beta}_1$, no entre los efectos aleatorios. Un valor cercano a 0 (como −0.14 en este caso) indica que las estimaciones son casi ortogonales entre sí. Una correlación alta en valor absoluto (> 0.7) sugeriría que centrar el predictor temporal (por ejemplo, usar `I(Days - mean(Days))` en lugar de `Days`) mejoraría la interpretación y la estabilidad numérica del modelo.

---

## REML vs ML: regla práctica

**Fitzmaurice et al. (2011, cap. 4)** presentan una discusión accesible sobre la diferencia entre máxima verosimilitud (ML) y máxima verosimilitud restringida (REML). La tabla siguiente resume las recomendaciones prácticas.

<DataTable
  caption="¿Cuándo usar REML y cuándo ML?"
  columns={["Objetivo", "Método", "Razón"]}
  rows={[
    ["Estimar componentes de varianza y reportar el modelo final", "REML (opción por defecto)", "REML corrige el sesgo en la estimación de τ² y σ² que se produce con ML en muestras finitas"],
    ["Comparar modelos con distintos efectos fijos (LRT)", "ML (REML = FALSE)", "REML transforma los datos y produce verosimilitudes no comparables cuando cambian las matrices de diseño"],
    ["Comparar modelos con mismos efectos fijos pero distinta estructura aleatoria", "REML (refit = FALSE)", "La verosimilitud restringida es comparable cuando la parte fija es idéntica"],
    ["Selección de modelos mediante AIC o BIC", "Preferentemente ML", "Aunque existe debate, la práctica más extendida en la literatura aplicada es usar ML para comparar modelos con distintos efectos fijos"]
  ]}
/>

<Callout type="tip" title="Regla de oro">
  Use REML para **todo lo que no sea comparar efectos fijos**. Cuando necesite una prueba de razón de verosimilitud entre modelos con distintos predictores, ajuste ambos con `REML = FALSE`, realice la comparación, y vuelva a REML para presentar las estimaciones finales del modelo seleccionado. Esta práctica es la recomendada por **Correa y Salazar (2025, cap. 3)** y por la mayoría de los textos aplicados.
</Callout>

---

## Valores ajustados y residuales

Una vez ajustado el modelo, conviene extraer los valores ajustados, los residuos y las predicciones de los efectos aleatorios (BLUPs). **Fitzmaurice et al. (2011, p. 203)** recomiendan examinar gráficamente los residuos para verificar los supuestos del modelo antes de proceder a la inferencia.

<RCell id="fitted-residuals" title="Extraer valores ajustados, residuales y BLUPs" code={`library(lme4)
library(ggplot2)
data(sleepstudy)

mod_B <- lmer(Reaction ~ Days + (Days | Subject),
              data = sleepstudy, REML = TRUE)

# Valores ajustados (incluyen el efecto aleatorio de cada sujeto)
sleepstudy$fitted   <- fitted(mod_B)

# Residuales (diferencia entre valor observado y ajustado)
sleepstudy$resid    <- resid(mod_B)

# Efectos aleatorios estimados (BLUPs): b0i y b1i para cada sujeto
ranef(mod_B)$Subject

# Visualización: ajuste por sujeto
ggplot(sleepstudy, aes(x = Days)) +
  geom_point(aes(y = Reaction), alpha = 0.4, size = 1.5) +
  geom_line(aes(y = fitted), color = "steelblue", linewidth = 1) +
  facet_wrap(~ Subject, ncol = 6) +
  scale_x_continuous(breaks = c(0, 3, 6, 9)) +
  labs(
    title    = "Rectas ajustadas del LMM por sujeto (Variante B)",
    subtitle = "Cada recta combina el efecto fijo poblacional y los BLUPs del sujeto",
    x = "Días de privación de sueño",
    y = "Tiempo de reacción (ms)"
  ) +
  theme_minimal(base_size = 9) +
  theme(strip.text = element_text(size = 8, face = "bold"),
        panel.grid.minor = element_blank())`} />

**Qué observar en este gráfico de ajustes individuales:**

- Las rectas **no son paralelas**: cada sujeto tiene su propio intercepto y su propia pendiente. Esto refleja la estructura de la Variante B y permite apreciar visualmente lo que aporta la inclusión de la pendiente aleatoria.
- Los sujetos cuya recta sube poco (pendiente cercana a 0 o incluso ligeramente negativa) son los más resistentes a la privación de sueño. Aquellos con rectas muy inclinadas son los más vulnerables.
- Compare mentalmente este gráfico con el que se obtendría de la Variante A (`(1 | Subject)`, rectas paralelas) para apreciar la diferencia. En el módulo de exploración gráfica se mostró cómo superponer ambas representaciones.

**Correa y Salazar (2025, cap. 5)** dedican una sección a la interpretación de los BLUPs, señalando que no deben tratarse como estimaciones insesgadas de los efectos individuales (tienen shrinkage hacia la media poblacional), pero sí como herramientas valiosas para la validación del modelo y la identificación de sujetos con trayectorias atípicas.

---

<Flashcards title="Conceptos del módulo" items={[
  {
    front: "¿Qué diferencia hay entre (Days | Subject) y (1 | Subject) + (0 + Days | Subject)?",
    back: "La primera forma (Variante B) estima la covarianza τ₀₁ entre intercepto y pendiente aleatorios; la segunda (Variante C) la fuerza a cero. Ambas incluyen τ₀² y τ₁², pero difieren en si permiten que el nivel basal y la tasa de cambio estén correlacionados en la población."
  },
  {
    front: "¿Qué es el ICC y qué valor indica que el modelo mixto es necesario?",
    back: "ICC = τ₀² / (τ₀² + σ²) es la fracción de la varianza total atribuible a diferencias entre sujetos. Un ICC > 0.10 ya justifica el modelo mixto frente a la regresión clásica. Valores cercanos a 0 sugieren que la agrupación es irrelevante y la regresión ordinaria es aceptable."
  },
  {
    front: "¿Por qué lme4 no reporta valores p para los efectos fijos?",
    back: "Por el debate metodológico sobre los grados de libertad correctos en modelos desbalanceados o con estructuras de covarianza complejas. Use lmerTest::lmer() para obtener p-valores basados en la aproximación de Satterthwaite, o calcule intervalos de confianza con confint()."
  },
  {
    front: "¿Qué son los BLUPs y para qué sirven?",
    back: "Best Linear Unbiased Predictors: las predicciones de los efectos aleatorios b₀ᵢ y b₁ᵢ para cada sujeto. Se obtienen con ranef() y tienen shrinkage hacia la media poblacional. Se usan para validar supuestos de normalidad de los aleatorios, para identificar sujetos atípicos y para hacer predicciones individuales, pero no se contrastan estadísticamente uno a uno."
  }
]} />

<Quiz questions={[
  {
    question: "En la salida de summary(lmer()), ¿qué representa 'Std.Dev. (Days, Subject)'?",
    options: [
      "El error estándar del coeficiente fijo de Days",
      "La desviación estándar de los efectos aleatorios de pendiente entre sujetos (√τ₁²)",
      "El residual estándar del modelo",
      "La correlación entre los efectos aleatorios"
    ],
    correctIndex: 1,
    explanation: "Std.Dev. en la sección 'Random effects' es √τ₁², la dispersión de las pendientes individuales alrededor de la pendiente media poblacional β₁. No debe confundirse con el Std. Error de los efectos fijos, que aparece en la sección 'Fixed effects' y cuantifica la incertidumbre sobre la estimación de β₁."
  },
  {
    question: "¿Cuándo es correcto usar anova(mod_A, mod_B, refit = FALSE)?",
    options: [
      "Cuando los modelos tienen distintos efectos fijos",
      "Cuando los modelos tienen los mismos efectos fijos pero distinta estructura aleatoria",
      "Siempre que se quiera comparar dos modelos mixtos",
      "Solo cuando ambos modelos se ajustaron con ML"
    ],
    correctIndex: 1,
    explanation: "refit = FALSE mantiene REML y es válido para comparar estructuras aleatorias anidadas que comparten los mismos efectos fijos. Para comparar efectos fijos se necesita ML (refit = TRUE, o bien ajustar ambos modelos con REML = FALSE desde el principio)."
  },
  {
    question: "Un spaghetti plot muestra líneas que parten de niveles distintos pero con pendientes muy similares. ¿Qué variante es la más adecuada por parsimonia?",
    options: [
      "Variante B: intercepto y pendiente aleatorios con correlación libre",
      "Variante C: intercepto y pendiente aleatorios sin correlación",
      "Variante A: solo intercepto aleatorio",
      "Regresión clásica sin efectos aleatorios"
    ],
    correctIndex: 2,
    explanation: "Perfiles paralelos indican que la heterogeneidad está en el nivel basal, no en la tasa de cambio. Agregar una pendiente aleatoria (Variante B o C) añade parámetros sin una justificación clara en los datos, violando el principio de parsimonia. La Variante A captura la heterogeneidad observada con el mínimo de parámetros adicionales."
  }
]} />

---

## Apéndice A — Bayes empírico y el fenómeno de *shrinkage*

Los BLUPs que obtuviste con `ranef()` en las secciones anteriores no son solo "residuales de grupo": son **predictores bayesianos empíricos**. Esta sección explica por qué, y cómo visualizar el efecto de *contracción* (*shrinkage*) hacia la media global.

### Los BLUPs como media a posteriori

En el modelo con intercepto aleatorio $b_{0i} \sim \mathcal{N}(0, \tau_0^2)$, la distribución a posteriori de $b_{0i}$ dado $\mathbf{y}_i$ es normal con media:

$$
\hat{b}_{0i}^{\text{BLUP}} = \underbrace{\frac{\tau_0^2}{\tau_0^2 + \sigma^2/n_i}}_{\lambda_i} \cdot (\bar{y}_i - \hat{\beta}_0)
$$

El factor $\lambda_i \in (0,1)$ es el **peso de shrinkage** del sujeto $i$:

- Si $\tau_0^2 \gg \sigma^2/n_i$ (mucha variabilidad entre sujetos, o sujeto bien medido): $\lambda_i \to 1$ y el BLUP se acerca a la media cruda del sujeto.
- Si $\tau_0^2 \ll \sigma^2/n_i$ (poca variabilidad entre sujetos, o sujeto poco medido): $\lambda_i \to 0$ y el BLUP se contrae hacia la media global $\hat\beta_0$.

Este es exactamente el **estimador de Bayes empírico** bajo pérdida cuadrática, y también coincide con las **Ecuaciones de Henderson** (1950).

<Callout type="info" title="¿Por qué 'empírico'?">
  Se llama Bayes *empírico* porque los hiperparámetros de la distribución a priori ($\tau_0^2$, $\sigma^2$) se **estiman de los datos** (vía REML o ML), en lugar de ser fijados antes de ver los datos como en el enfoque bayesiano pleno. Si usáramos MCMC con distribuciones a priori prefijadas y `brms`, obtendríamos intervalos de credibilidad completos para cada $b_{0i}$, a costa de mayor costo computacional.
</Callout>

<RCell id="bayes-shrinkage" title="Visualizar el shrinkage: medias crudas vs BLUPs" code={`library(lme4)
library(ggplot2)

data(sleepstudy, package = "lme4")

# Modelo con pendiente aleatoria
mod <- lmer(Reaction ~ Days + (Days | Subject), data = sleepstudy, REML = TRUE)

# --- Medias crudas por sujeto ---
medias_crudas <- tapply(sleepstudy$Reaction, sleepstudy$Subject, mean)

# --- BLUPs del intercepto ---
re <- ranef(mod)$Subject
blups_int <- re[["(Intercept)"]]
names(blups_int) <- rownames(re)

# --- Media global del intercepto (efecto fijo) ---
media_global <- fixef(mod)["(Intercept)"]

# --- Data frame para ggplot ---
df_shrink <- data.frame(
  sujeto       = names(medias_crudas),
  media_cruda  = as.numeric(medias_crudas),
  blup_int     = blups_int + media_global,   # BLUP en escala original
  media_global = media_global
)

# Gráfico de shrinkage
ggplot(df_shrink, aes(x = media_cruda, y = blup_int)) +
  geom_abline(slope = 1, intercept = 0, color = "gray60", linetype = "dashed",
              linewidth = 0.8) +
  geom_hline(yintercept = media_global, color = "steelblue", linetype = "dotted",
             linewidth = 1) +
  geom_point(size = 3, color = "darkred") +
  geom_segment(aes(xend = media_cruda, yend = media_global),
               color = "gray70", linewidth = 0.4) +
  annotate("text", x = min(df_shrink$media_cruda) + 5,
           y = media_global + 5, label = "Media global",
           color = "steelblue", size = 3.5) +
  labs(
    title = "Shrinkage: medias crudas vs. BLUPs (intercepto)",
    subtitle = "Los BLUPs se contraen hacia la media global — mayor contracción para sujetos extremos",
    x = "Media cruda por sujeto (ms)",
    y = "BLUP del intercepto (ms)",
    caption = "Línea discontinua = identidad (sin shrinkage) | Línea punteada azul = media global"
  ) +
  theme_bw()

# Magnitud del shrinkage
df_shrink$shrinkage <- abs(df_shrink$media_cruda - df_shrink$blup_int)
cat("\\nShrinkage por sujeto (ms):\\n")
print(round(df_shrink[order(-df_shrink$shrinkage), c("sujeto","media_cruda","blup_int","shrinkage")], 2))`}
/>

<RCell id="bayes-lambda" title="Calcular el factor lambda de shrinkage" code={`library(lme4)
data(sleepstudy, package = "lme4")
mod_a <- lmer(Reaction ~ Days + (1 | Subject), data = sleepstudy, REML = TRUE)

# Extraer componentes de varianza
tau2  <- as.numeric(VarCorr(mod_a)$Subject)  # varianza entre sujetos
sig2  <- sigma(mod_a)^2                        # varianza residual
ni    <- table(sleepstudy$Subject)              # observaciones por sujeto

# Factor lambda (shrinkage weight)
lambda <- tau2 / (tau2 + sig2 / as.numeric(ni))

cat("=== FACTOR LAMBDA (peso del shrinkage) ===\\n")
cat(sprintf("tau² (entre sujetos): %.4f\\n", tau2))
cat(sprintf("sigma² (residual):    %.4f\\n", sig2))
cat(sprintf("n_i (obs/sujeto):     %d\\n\\n", ni[1]))

cat("Lambda = tau² / (tau² + sigma²/n_i)\\n")
cat(sprintf("Lambda = %.4f / (%.4f + %.4f/%.0f) = %.4f\\n",
            tau2, tau2, sig2, ni[1], lambda[1]))
cat(sprintf("\\nInterpretación: el BLUP captura el %.0f%% de la desviación cruda del sujeto\\n",
            lambda[1] * 100))
cat("El %.0f%% restante se 'encoge' hacia la media global.\\n",
    (1 - lambda[1]) * 100)`}
/>

---

## Apéndice B — El algoritmo EM: convergencia visual paso a paso

El algoritmo EM (Expectation-Maximization, Dempster, Laird y Rubin, 1977) es uno de los métodos clásicos para estimar modelos con datos incompletos o variables latentes. En los LMM, los efectos aleatorios $\mathbf{b}_i$ son las "variables latentes" que el algoritmo EM maneja de manera implícita.

### Los dos pasos del algoritmo

**Paso E (Expectation):** calcula $Q(\theta | \theta^{(t)}) = \mathbb{E}_{\mathbf{b}|\mathbf{y},\theta^{(t)}}[\log L(\theta; \mathbf{y}, \mathbf{b})]$, es decir, la verosimilitud esperada sobre la distribución condicional de los efectos aleatorios dados los datos y los parámetros actuales.

**Paso M (Maximization):** maximiza $Q$ respecto a $\theta$, produciendo la nueva estimación $\theta^{(t+1)}$.

Para el modelo de intercepto aleatorio balanceado ($n_i = n$ para todo $i$, $N$ sujetos), las actualizaciones cerradas del EM son:

$$
\hat\sigma^{2,(t+1)} = \frac{1}{Nn} \sum_{i,j} \left[(y_{ij} - \hat\beta^{(t)} - \hat b_i^{(t)})^2 + \text{Var}(b_i | \mathbf{y})\right]
$$

$$
\hat\tau^{2,(t+1)} = \frac{1}{N} \sum_i \left[(\hat b_i^{(t)})^2 + \text{Var}(b_i | \mathbf{y})\right]
$$

<RCell id="em-algorithm" title="Algoritmo EM para intercepto aleatorio — Visualización de convergencia" code={`library(lme4)
library(ggplot2)

set.seed(2024)
data(sleepstudy, package = "lme4")

# Usar solo días 0-4 para simplificar (modelo de intercepto aleatorio)
dat <- sleepstudy[sleepstudy$Days <= 4, ]
N   <- length(unique(dat$Subject))  # número de sujetos
n   <- 5                             # mediciones por sujeto (días 0-4)

y_mat <- matrix(dat$Reaction, nrow = N, ncol = n, byrow = TRUE)
y_bar  <- rowMeans(y_mat)           # medias por sujeto
y_grand <- mean(y_mat)              # media global

# ---- Implementación del algoritmo EM ----
em_lmm <- function(y_mat, n_iter = 50, sigma2_0 = 1000, tau2_0 = 500) {
  N <- nrow(y_mat)
  n <- ncol(y_mat)
  y_bar   <- rowMeans(y_mat)
  y_grand <- mean(y_mat)

  sigma2 <- sigma2_0
  tau2   <- tau2_0
  beta   <- y_grand

  historia <- data.frame(iter = 0, sigma2 = sigma2, tau2 = tau2,
                          loglik = NA, beta = beta)

  for (t in 1:n_iter) {
    # ----- Paso E: calcular BLUPs y sus varianzas -----
    lambda   <- tau2 / (tau2 + sigma2 / n)
    b_hat    <- lambda * (y_bar - beta)
    var_b    <- (1 - lambda) * tau2   # Var(b_i | y_i)

    # ----- Paso M: actualizar parámetros -----
    beta_new   <- y_grand - mean(b_hat)
    resid2     <- rowMeans((y_mat - matrix(beta_new + b_hat, N, n))^2)
    sigma2_new <- mean(resid2) + var_b
    tau2_new   <- mean(b_hat^2) + var_b

    # Log-verosimilitud marginal (para monitorear convergencia)
    V_i <- tau2_new + sigma2_new / n
    ll  <- -N/2 * log(2 * pi * V_i) - sum((y_bar - beta_new)^2) / (2 * V_i)

    sigma2 <- sigma2_new
    tau2   <- tau2_new
    beta   <- beta_new

    historia <- rbind(historia,
      data.frame(iter = t, sigma2 = sigma2, tau2 = tau2, loglik = ll, beta = beta))
  }
  historia
}

# Ejecutar EM con valores iniciales deliberadamente lejanos
hist_em <- em_lmm(y_mat, n_iter = 40, sigma2_0 = 5000, tau2_0 = 100)

# Comparar con lmer (solución exacta)
mod_lmer <- lmer(Reaction ~ 1 + (1 | Subject), data = dat, REML = FALSE)
sigma2_lmer <- sigma(mod_lmer)^2
tau2_lmer   <- as.numeric(VarCorr(mod_lmer)$Subject)

cat("=== COMPARACIÓN EM vs lmer ===\\n")
cat(sprintf("EM (última iter)   sigma² = %.2f | tau² = %.2f\\n",
            tail(hist_em$sigma2, 1), tail(hist_em$tau2, 1)))
cat(sprintf("lmer (referencia)  sigma² = %.2f | tau² = %.2f\\n",
            sigma2_lmer, tau2_lmer))

# Gráfico de convergencia
p_sigma <- ggplot(hist_em[-1, ], aes(x = iter, y = sigma2)) +
  geom_line(color = "steelblue", linewidth = 1) +
  geom_hline(yintercept = sigma2_lmer, linetype = "dashed", color = "darkred") +
  annotate("text", x = 35, y = sigma2_lmer + 50, label = "lmer", color = "darkred") +
  labs(title = "Convergencia de sigma² (varianza residual)",
       x = "Iteración EM", y = "sigma²") +
  theme_bw()

p_tau <- ggplot(hist_em[-1, ], aes(x = iter, y = tau2)) +
  geom_line(color = "darkorange", linewidth = 1) +
  geom_hline(yintercept = tau2_lmer, linetype = "dashed", color = "darkred") +
  annotate("text", x = 35, y = tau2_lmer + 30, label = "lmer", color = "darkred") +
  labs(title = "Convergencia de tau² (varianza entre sujetos)",
       x = "Iteración EM", y = "tau²") +
  theme_bw()

gridExtra::grid.arrange(p_sigma, p_tau, ncol = 2)`}
/>

<Callout type="info" title="EM en la práctica vs. Newton-Raphson">
  El EM es conceptualmente elegante pero converge **lentamente** cerca de la solución (convergencia lineal). Los paquetes modernos como `lme4` usan el **algoritmo de Newton-Raphson** o variantes tipo gradiente conjugado (BOBYQA) que tienen convergencia cuadrática y son mucho más rápidos. Sin embargo, el EM es útil cuando el paso M tiene una forma cerrada y es más estable en regiones alejadas del óptimo.
</Callout>

---

## Referencias del módulo

- **Correa, J. C. y Salazar, J. C. (2025).** *Modelos Mixtos.* Universidad Nacional de Colombia, Sede Medellín, Facultad de Ciencias, Departamento de Estadística. [Disponible en el repositorio del curso].
- **Fitzmaurice, G. M., Laird, N. M. y Ware, J. H. (2011).** *Applied Longitudinal Analysis* (2.ª ed.). John Wiley & Sons. [Capítulos 4, 5 y 8: especificación del LMM, estimación REML/ML e interpretación de salidas].
- **Pinheiro, J. y Bates, D. (2000).** *Mixed-Effects Models in S and S-PLUS.* Springer. [Texto de referencia del paquete `nlme`; capítulos 1 y 2 contienen una exposición rigurosa de la notación y las estructuras de covarianza].
- **Bates, D., Mächler, M., Bolker, B. y Walker, S. (2015).** Fitting linear mixed-effects models using lme4. *Journal of Statistical Software*, 67(1), 1–48. [Documentación técnica de `lme4`; incluye recomendaciones sobre optimizadores y manejo de advertencias de convergencia].