/**
 * URL segment that identifies the only course currently hosted.
 * Route: /cursos/[course]/modulos/[slug]
 */
export const COURSE_SLUG = 'modelos-mixtos' as const;

/** Human-readable label used in breadcrumbs and aria attributes. */
export const COURSE_LABEL = 'Curso' as const;
